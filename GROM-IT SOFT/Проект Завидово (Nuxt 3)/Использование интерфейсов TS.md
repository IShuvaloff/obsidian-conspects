
1. Для загружаемых по API-запросам данных нужно:
	1. создавать интерфейсы для объекта в списке, например, `interface IPartnerCard`, который будет использоваться в функциях возврата загружаемых данных как `IPartnerCard[]`;
	2. создавать интерфейсы для объекта в карточке (на странице объекта), причем двумя разными способами:
		1. либо просто расширить дополнительными полями интерфейс для объекта в списке, например, `interface IPartnerCard` - причем даже когда в списке по данному интерфейсу возвращаться будет меньше данных, тогда как поля все обязательные, ошибок не будет;
		2. либо создать дополнительный интерфейс `interface IPartnerPage extends IPartnerCard`, в котором дописать недостающие данные, и использовать его уже на отдельных страницах объекта;
	3. 
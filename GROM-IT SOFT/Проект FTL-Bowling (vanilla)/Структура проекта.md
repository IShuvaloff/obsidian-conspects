#native #vanilla

1. Проект выполнен на ванильных HTML/CSS/JS;
2. В проекте используются некоторые плагины, устанавливаемые через Node.js^[использование плагинов в нативном проекте очень просто: устанавливается плагин через `npm i <plugin>`, а затем в папке `node_modules/<plugin>/` отыскивается (см. документацию) что-то вроде подпапки `/public` с файлами css/js, пути к которым и прописываются в виде `<style>/<script>` в блоге `<head>` проекта (аналогично подключению через CDN)]:
	1. `choices.js` - кастомный комбобокс:
	   
	   ![[Pasted image 20240515095456.png]]
	   
	2. `splide.js` - слайдер;
	3. `maska` - маска для инпутов.

Следует помнить некоторые ==правила подключения плагинов в нативном проекте==:
1. ссылки на внешние скрипты и стили нужно прописывать в блоке `<head>` ТОЛЬКО перед подключением своих файлов со скриптами и стилями:
   
   ![[Pasted image 20240515095714.png]]
   
2. 
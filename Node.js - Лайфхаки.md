
1. `npm i` = "install" - установить проект, используя файлы `package.json` и `package-lock.json` (из последнего берутся именно те версии плагинов, с которыми 100% данное приложение будет работать, а не последние версии, прописанные в первом);
2. `npm ci` = "clean install" - очистить проект с полным удалением директории `.node_modules` и установкой всех зависимостей с чистого листа. Требуется, когда, например, *заменили некоторые файлы в зависимостями целиком* и требуется заново обновить весь проект;
3. 
#robots #seo

1. файл для **[настроек индексации](https://timeweb.com/ru/community/articles/chto-takoe-robots-txt)** - `robots.txt`^[требуется для того, чтобы роботы не блуждали по сайту как им вздумается и не посещали страниц с мусором, дубляжами и неуникальными данными]:
	* заполняются 2-3 обязательных параметра - `User-agent` и `Allow/Disallow`:
	  
	  ![[Pasted image 20240110103433.png]]
	  
	* файл выкладывается в папку `public` либо `src/public` при наличии таковой;
	* примеры содержимого (следует учесть, что файл читается сверху вниз и настройки `Allow/Disallow` применяются для того `User-agent`, что указан перед ними):
	  
	  ![[Pasted image 20240110104220.png|300]]
	  
2. Добавление [метатегов](https://nuxt.com/docs/getting-started/seo-meta) в проект:
	1. средствами композаблов **`useSeoMeta`** (для *динамической реактивной обработки*, в т.ч. на клиенте и при изменяющихся заголовках в процессе работы) и **`useServerSeoMeta`** (для *единоразовой статической обработки* заголовков при SSR, когда не требуется их изменение в процессе работы):

		![[Pasted image 20240122114929.png]]

	2. Метатег **`keywords`** в настоящее время потерял актуальность и не требуется;
	3. Заголовок **`h1`** обязателен на каждой странице. Особенности:
		* нельзя делать его невидимым, т.к. современные поисковики научились определять такие теги как недобросовестные;
		* нужно добавлять в `body`, т.к. непосредственно метатега не существует;
		* см. пример на главной странице проекта "TDS Group":
		  
		  ![[Pasted image 20240122115238.png]]
		  ![[Pasted image 20240122115253.png]]
		  Здесь в случае наличия SEO-заголовка берется он, в ином случае подтягиваются обычные заголовки, указанные в админке сайта. Причем для тега `h1` указаны специфические стили, подразумевающие наличие тега `<br />` внутри;
	4. если мета-теги статичные для всего проекта, необязательно добавлять их приведенными выше способами для каждой из страниц. Можно выставить сразу настройки в файле конфигурации `nuxt.config.ts` путем добавления мета (на примере подключения **google-** и **yandex-метрик**):
	   
		```ts
		app: { 
		  head: { 
			meta: [
			  { name: 'yandex-verification', content: '8f666157dc879af2'},
			  { name: "google-site-verification", content: "2WPp1iYtRdVh4Gt2-MNvvpeKuCd67avfoQLIahweRsM" },
			]
		  }
		},
		```
 		  		  
5. Полный процесс создания SEO применяется в проекте OFFPRICE, порядок следующий:
	1. в глобальном миддлваре идет синхронная (через await!!!) загрузка данных СЕО для конкретного маршрута/страницы;
	2. в случае наличия производится сохранение данные через `useHead` (см. спец. функцию `updatePageHead`), либо берутся дефолтные данные, сохраненные в хранилище. Т.о. обеспечивается глобальность назначения СЕО-параметров на каждой страницу;
	3. данные по СЕО сохраняются в спец. переменную в хранилище, чтобы в случае наличия ранее загруженных данных не выполнять повторно запрос;
	4. при необходимости обновить данные для конкретно взятой страницы можно повторно запустить функцию обновления тегов `updatePageHead`: теги на странице реактивные и обновятся автоматически (проверить можно, открыв "Код страницы").
6. Добавление **канонического URL** для всех страниц;

7. Подключение Yandex-метрики (см. Завидово)
8. Подключение Google-метрики (см. Завидово)

### Примеры подключения Яндекс-метрики
Смотреть в [[yandex-verification.pdf|нейросетевом отчете]].

### Ограничения индексации поисковиками
Смотреть в [[seo-indexing.pdf|нейросетевом отчете]].

## Редиректы для SEO-оптимизации
Использование **серверных миддлваров** (`/server/middleware`)^[см. проект "Завидово", миддлвар `/server/middleware/redirects.ts`]. Срабатывает *только один раз при серверном рендеринге*. Идеально для:
1. редиректов (301) для исключения индексации первичного варианта URL роботами;
2. добавления/удаления последних слэшей из URL (в случае, если версия Nuxt не поддерживает опцию в конфигурации `router: { trailingSlash: true }`);

Использование **клиентских миддлваров** (`/middleware`)^[см. проект "Завидово", миддлвар `/middleware/02.trailing-slash.global.ts` - для добавления последних слэшей в адресную строку]. Особенности:
1. Срабатывает для любой клиентской навигации (в рамках уже отрендеренного сайта), когда обращения к серверному миддлвару уже не производится;
2. Оптимально для редактирования query-параметров, например, указания инициализирующего шага визарда при загрузке страницы;
3. Для случаев, когда требуется добавить/удалить trailing slash в URL, подойдет только такой вариант.

# TODO: добавить единый блок с SEO, метриками и стилизацией